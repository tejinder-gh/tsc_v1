"use client";

/**
 * Japanese Archival Paper Grain Overlay
 * Renders a fixed, GPU-accelerated micro-noise layer across the warm editorial canvas.
 * Gives the browser surface the tactile feel of physical archival stock (Stripe Press / Locomotive tier).
 */
export function PaperGrain() {
  return <div aria-hidden="true" className="paper-grain" />;
}
