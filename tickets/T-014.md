# T-014 — Synchronize DESIGN.md specification with canonical Geist editorial tokens

**Source finding:** AUDIT_REPORT.md DES-02 | Severity: P2 | Class: SUBOPTIMAL  
**Assigned tier:** INTERMEDIATE  
**Effort estimate:** 1h | **Sequence:** independent

## WHAT
Update `DESIGN.md` to reflect the active, canonical editorial design system deployed across the application. Replace legacy descriptions asserting Display: Poppins and Body: DM Sans with the active Geist Sans (`--font-geist-sans`) and Geist Mono (`--font-geist-mono`) typography stack. Reconcile the "Anti-template self-critique" section to embrace the warm paper canvas (`#f4f1e9`), deep ink (`#12130f`), and signal lime (`#d5ff52`) palette, clearly delineating the legacy navy/blue tokens as backward-compatible aliases.

## WHY
The audit's Design review revealed documentation drift: `DESIGN.md` still described an earlier visual iteration (Poppins display font and a rule against "cream paper and acid green"), while the entire production codebase was intentionally refactored to an editorial studio aesthetic featuring warm paper, Geist typography, and signal lime status pills. Keeping the design documentation synchronized ensures future agent sessions and human contributors adhere to the real design rules without creating conflicting styles.

## SCOPE — FILES
IN:
- `DESIGN.md`

OUT:
- `app/globals.css` — CSS variables are already authoritative; do not alter.
- Any file in `components/**` or `app/**`.

## ACCEPTANCE CRITERIA
- [ ] AC1: `DESIGN.md` accurately lists Geist Sans and Geist Mono as the primary typography tokens (`--font-geist-sans`, `--font-geist-mono`, and `--font-geist`), detailing their usage hierarchy.
- [ ] AC2: The Canonical Semantic Palette section matches the active tokens in `app/globals.css`: `--tsc-ink`, `--tsc-paper`, `--tsc-surface`, `--tsc-line`, `--tsc-line-strong`, `--tsc-muted`, `--tsc-signal`, `--tsc-action`, `--tsc-positive`.
- [ ] AC3: The "Anti-template self-critique" is reconciled with the editorial direction (approachable precision, editorial technical journal, intentional monospaced accents).
- [ ] AC4: Legacy aliases (navy, blue, slate, mist) are documented clearly as transitional tokens.

## TEST PLAN
1. Read `DESIGN.md` and cross-reference each token against `app/globals.css` and `app/layout.tsx`.
2. Confirm typography and color descriptions match the actual rendered UI.

## CONSTRAINTS
- Markdown documentation update only. No code or styling changes.

## ROLLBACK
`git checkout -- DESIGN.md`

## BLOCKED PROTOCOL
None.
