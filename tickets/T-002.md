# T-002 — Re-anchor site pricing models and copy to enterprise tiers ($15k–$200k)

**Source finding:** AUDIT_REPORT.md §PHASE 5.1 | Severity: P1 | Class: SUBOPTIMAL
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 1.5h | **Sequence:** independent

## WHAT
Update `pricing` anchors and details in `content/site.ts` and `app/pricing.md/route.ts` to replace the "$395/month" commodity freelancer framing with institutional enterprise-grade tiers:
- **Diagnostic & Scoped Blueprint:** $2,500 – $5,000 (Credited in full toward project build)
- **Bespoke System Deployment:** $15,000 – $75,000 (Single bottleneck to full-practice automation infrastructure)
- **Managed AI Operations & Retainer:** $2,500 – $12,500/month (Ongoing monitoring, SLA, model fine-tuning, compliance)
- **Enterprise Digital Transformation:** $100,000 – $250,000+

## WHY
Prominently displaying "$395/month" triggers suspicion among mid-market clinic networks and commercial firms regarding security, uptime, and engineering caliber. Anchoring at $15k–$75k aligns pricing with high-ticket value delivery.

## SCOPE — FILES
IN:
- `content/site.ts`
- `app/pricing.md/route.ts`

OUT:
- `app/api/*` (API endpoints consume types, no copy logic)
- `components/home/*` (components read dynamically from `content/site.ts`)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: `content/site.ts` `pricing.local` and `pricing.practice` constants reflect the new tiered structure without mentioning $395/month.
- [ ] AC2: Visiting `/pricing.md` outputs markdown with the updated enterprise tiers, SLA terms, and credit policy.
- [ ] AC3: All existing TypeScript interfaces (`PricingAnchorContent`, etc.) remain backward-compatible with zero type errors.
- [ ] AC4: `bun run test` passes with zero regression.

## TEST PLAN
1. Run `bun test` to ensure existing schema tests pass.
2. Run `curl -s http://localhost:3000/pricing.md` and verify the output contains the new enterprise anchors.
3. Run `bun run build` to confirm static generation of `/pricing.md` succeeds.

## CONSTRAINTS
- Do not remove existing segment keys (`local` and `practice`) to avoid breaking components expecting these keys.
- Preserve all existing interfaces and exports.

## ROLLBACK
Revert `content/site.ts` and `app/pricing.md/route.ts` via `git checkout`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
