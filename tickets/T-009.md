# T-009 — Purge orphaned `SolutionPlaceholder.tsx` and legacy font definitions

**Source finding:** AUDIT_REPORT.md §PHASE 0.3 | Severity: P3 | Class: SUBOPTIMAL
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 1.0h | **Sequence:** independent

## WHAT
Clean up dead prototype artifacts left behind by earlier development iterations:
1. Delete `components/journey/opportunity/SolutionPlaceholder.tsx`.
2. Remove its unused export from `components/journey/opportunity/index.ts`.
3. Remove unused legacy `@font-face` definitions for Poppins and DM Sans from `app/globals.css:20-58` (the application exclusively uses Next.js Geist fonts).
4. Run Biome check to fix formatting in touched files.

## WHY
Eliminates code bloat, avoids unnecessary font preloading/parsing by browsers, and cleans up the agent confession log to ensure zero dead code in production.

## SCOPE — FILES
IN:
- `components/journey/opportunity/SolutionPlaceholder.tsx` (delete)
- `components/journey/opportunity/index.ts`
- `app/globals.css`

OUT:
- `components/journey/opportunity/OpportunityView.tsx` (active view; do not touch)
- `components/journey/demonstration/*` (active views; do not touch)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: `components/journey/opportunity/SolutionPlaceholder.tsx` is deleted.
- [ ] AC2: `components/journey/opportunity/index.ts` exports only active components.
- [ ] AC3: `app/globals.css` no longer contains unused `@font-face` blocks for Poppins and DM Sans.
- [ ] AC4: `bun run test` passes 100% and `bun run build` succeeds without warnings.

## TEST PLAN
1. Run `bun test` to ensure no imports were broken.
2. Run `bun run build` to verify clean build.

## CONSTRAINTS
- Do not modify active theme color variables or font-geist utilities in `app/globals.css`.

## ROLLBACK
Restore files via `git checkout`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
