# T-022 — Restore the Biome lint gate

**Source finding:** AUDIT_REPORT.md §F-07 | Severity: P1 | Class: BROKEN
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 1.5 hours | **Sequence:** independent

## WHAT

Make `npm run lint` pass by excluding the generated `.automations/**` runtime store from Biome and applying Biome’s required import organization to the two current source errors. Do not format, delete, or otherwise mutate the runtime JSON data. Address only the nine current errors; non-blocking warnings are explicitly deferred.

## WHY

The CI quality command currently fails after ordinary tests/runtime operations because generated ignored files are linted. That blocks reliable handoff and makes a passing test suite misleading.

## SCOPE — FILES

IN:
- `biome.json` — add a precise `.automations` exclusion compatible with the current include convention.
- `app/dashboard/drafts/page.tsx` — apply the import ordering required by Biome.
- `app/invite/[token]/invite-card.tsx` — apply the import ordering required by Biome, unless T-017 has already deleted this file; if T-017 lands first, remove this path from the implementation and state why in the ticket result.

OUT:
- `.automations/**` — generated/user runtime state; do not edit, delete, or format it.
- All 62 current warnings — defer them; do not expand into lint-style cleanup.
- Biome rule severity, package scripts, dependencies, and unrelated source files — no configuration broadening or rule suppression.

## ACCEPTANCE CRITERIA (testable, binary)

- [x] AC1: `npm run lint` exits 0 with no errors after the current `.automations` runtime files are present.
- [x] AC2: Biome does not report files beneath `.automations/` when lint is run.
- [x] AC3: The two specified source files have no import-organization diagnostic; if the invite file was removed by T-017, no source references it and no substitute file is changed.
- [x] AC4: The change does not alter `.automations/**` content or tracked application behavior.
- [x] AC5: `npm test` and `npx tsc --noEmit` remain exit 0.

## TEST PLAN

1. Run `npm run lint`; expect exit 0.
2. Run `npm test`; expect exit 0.
3. Run `npx tsc --noEmit`; expect exit 0.
4. Run `git diff -- .automations` (or inspect `git status --ignored` only) to confirm no runtime data was intentionally changed; `.automations` is ignored, so record the boundary rather than claiming a tracked diff proves it.

## CONSTRAINTS

- Use the repository’s existing Biome configuration syntax; do not add ESLint or another formatter.
- Do not hide source errors with broad `ignoreUnknown`, global rule disablement, or a blanket `!**/*.json` exclusion.
- Keep the source edits formatter-only/import-order-only.

## ROLLBACK

Revert the small configuration/source commit. Do not solve a future generated-file failure by deleting runtime state.

## BLOCKED PROTOCOL

If Biome’s current version cannot express a narrow `.automations` exclusion, STOP and report the supported configuration alternatives. Do not silently exclude all JSON or all application paths.
