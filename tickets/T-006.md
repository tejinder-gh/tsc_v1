# T-006 — Add an FAQ entry addressing agency vs. self-serve SaaS

**Source finding:** AUDIT_REPORT.md CPO §2B / CMO §4A | Severity: P1 | Class: MISSING
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 1.5h | **Sequence:** independent

## WHAT
Add one new entry to `content/faq.ts`'s `homeFaq` array answering the implicit objection "why hire an agency instead of signing up for a cheaper AI receptionist SaaS tool," grounded only in facts already established elsewhere in this codebase (bespoke per-business builds across 21 services / 26 industries, a monitored/maintained engine with same-business-day fix response already promised in the existing "What happens if an automation breaks?" FAQ entry, human-audited onboarding, tool-agnostic integration).

## WHY
Live competitor research (AUDIT_REPORT.md CMO §4A) found several self-serve SaaS competitors (Emitrr, My AI Front Desk, Goodcall, Rosie, OnceHub) priced at $100–$500/month with an overlapping surface pitch ("missed calls answered," "booking reminders"). Nothing on the site currently addresses this comparison, even implicitly — the existing FAQ (`content/faq.ts`) covers cost, timeline, integrations, privacy, reliability, and technical burden, but not "why not just buy the cheaper tool." This is a pure content addition using facts the site already asserts elsewhere, not new claims.

## SCOPE — FILES
IN:
- `content/faq.ts`

OUT:
- `components/Faq.tsx` — the accordion component already renders whatever's in the array; do not modify rendering logic, this is content-only.
- Any other FAQ arrays (industry-page FAQs in `content/industries.ts`, service-page FAQs in `content/services.ts`) — those are scoped to specific industries/services and a generic agency-vs-SaaS answer doesn't fit their per-page framing; adding it there is a larger, separately-scoped content pass.
- Competitor names — do not name specific competitors (Emitrr, etc.) in the copy; compare against the category ("off-the-shelf AI answering tools") in general terms, consistent with how the rest of the site's copy never names competitors.

## ACCEPTANCE CRITERIA
- [ ] AC1: `homeFaq` has one new `FaqItem` entry (matching the existing `{ q, a }` shape) addressing the build-vs-buy question.
- [ ] AC2: The answer references only facts already stated elsewhere in `content/` (bespoke build, monitored/maintained per the existing "What happens if an automation breaks?" entry, human audit call, tool-agnostic integration per the existing "Do you work with the tools I already have?" entry) — no new numeric claims, no invented statistics, no competitor names.
- [ ] AC3: The new entry's tone and length match the existing five entries (1–3 sentences, same register — plain, direct, no superlatives like "the best" or "revolutionary").
- [ ] AC4: `npm run lint` passes (Biome would flag obvious formatting issues; this also catches accidental type errors against the `FaqItem` interface).
- [ ] AC5: `npm run dev`, load `/`, visually confirm the new FAQ entry renders correctly in the accordion (opens on click, closes others, matches existing entries' styling — no component changes needed if content-only, but verify nothing broke).

## TEST PLAN
1. `npm run lint`.
2. `npm run dev`, navigate to `/`, scroll to the FAQ section, click through all entries including the new one — confirm single-open-accordion behavior (already built, per `SUMMARY.md`) still works and the new entry displays correctly at both desktop and mobile widths.
3. Read the new answer aloud against AC2's fact-sourcing constraint — if any sentence can't be traced to an existing `content/*.ts` claim, rewrite it before considering this done.

## CONSTRAINTS
- Follow the exact `FaqItem` shape and array style already in `content/faq.ts` — plain typed const, no new interfaces or helper functions.
- No fabricated numbers or claims — this is the same "no invented statistics" discipline documented in `content/proof.ts`'s header comment, applied here to comparative claims about competitors-as-a-category.
- No new dependencies.
- Placement: append as the last entry (after "Do I need technical staff to use this?") unless a clear ordering reason argues otherwise — if you reorder, note why in your final report.

## ROLLBACK
Revert the single-entry addition to `content/faq.ts`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
