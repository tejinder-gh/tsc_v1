# T-007 — Fix input-border contrast on the contact form

**Source finding:** AUDIT_REPORT.md Phase 3 (Design) | Severity: P2 | Class: SUBOPTIMAL
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 1.5h | **Sequence:** independent

## WHAT
Introduce a new CSS token for form-input borders (distinct from the existing `--color-line`, which stays as-is for hairlines/dividers/card borders — its correct, intended use per `DESIGN.md`) with a value that meets the WCAG 1.4.11 non-text-contrast threshold of 3:1 against white, and swap it in everywhere `border-line` is currently used specifically for a text-input's border (not for cards, dividers, or hairlines).

## WHY
`DESIGN.md`'s own token table assigns `--color-line` (#DDE3EE) to both "hairline borders/dividers" and, in practice, input borders (`components/forms/ContactForm.tsx`'s `fieldBaseClass`) — but #DDE3EE on white measures 1.29:1, well under the 3:1 threshold WCAG 1.4.11 requires for UI-component borders like form inputs (hairline dividers aren't held to this threshold, which is why the card/divider uses are fine as-is). This was already surfaced and explicitly left open in the same-day design pass (`SUMMARY.md`, "flagged here as a borderline WCAG 1.4.11 call for you to weigh in on") — this ticket resolves it by adding a dedicated, correctly-contrasted token rather than overloading `--color-line` with two different contrast requirements.

In `app/globals.css`, add one new token under `@theme` (e.g. `--color-border-input`); do not change `--color-line`'s existing value. In `components/forms/ContactForm.tsx`, swap `border-line` to the new token in `fieldBaseClass` only (around line 48). In `app/dev/components/page.tsx`, swap the equivalent input example (around line 146) so the internal component gallery reflects the corrected real pattern, per its own stated purpose of mirroring real primitives.

## SCOPE — FILES
IN:
- `app/globals.css`
- `components/forms/ContactForm.tsx`
- `app/dev/components/page.tsx`

OUT:
- Every other `border-line` usage (`app/how-it-works/page.tsx`, `app/results/page.tsx`, `app/dev/components/page.tsx`'s card/section examples, `components/Header.tsx`, `components/Faq.tsx`) — these are cards, dividers, and hairlines, which `DESIGN.md` correctly scopes `--color-line` for and which are NOT held to the 3:1 UI-component threshold. Do not touch these; changing them would be scope creep against a token that is working as designed there.
- `RoiCalculator.tsx`'s form input (line ~173, `border-white/30`) — that's on a dark navy background, a different contrast calculation entirely, and not the finding this ticket addresses.
- `--color-line`'s own defined value in `app/globals.css` — do not change it; a new token is the fix, not a redefinition of the existing one (redefining it would silently change every hairline/card border sitewide, which is explicitly out of scope and would break the card/divider uses that currently work correctly).

## ACCEPTANCE CRITERIA
- [ ] AC1: A new token exists in `app/globals.css`'s `@theme` block with a hex value that computes to ≥3:1 contrast against `#FFFFFF` (white) — show your contrast calculation (e.g. via a WCAG contrast formula, not eyeballed) in your final report, matching the rigor `SUMMARY.md` describes ("calculated from the actual token hex values, script-based, not eyeballed").
- [ ] AC2: `components/forms/ContactForm.tsx`'s `fieldBaseClass` uses the new token instead of `border-line`; the error-state override (`fieldErrorClass`, using `border-danger`) is untouched and still takes visual precedence when a field has an error.
- [ ] AC3: `app/dev/components/page.tsx`'s input example (line ~146) uses the same new token, so the gallery accurately represents the real, corrected pattern.
- [ ] AC4: Every other `border-line` usage in the codebase (cards, dividers, hairlines) is byte-identical to before this change — `git diff` must show changes only in the three files listed under IN.
- [ ] AC5: Visually, the new border is a subtle darkening from the previous #DDE3EE — not a jarring change; it should still read as "soft input border," just meeting the contrast floor, consistent with `DESIGN.md`'s stated direction ("approachable precision," not heavy/harsh borders).
- [ ] AC6: `npm run lint` passes and `npm run build` succeeds.

## TEST PLAN
1. Compute the contrast ratio of your chosen hex value against `#FFFFFF` using the standard WCAG relative-luminance formula (show the math in your report — do not guess-and-check visually).
2. `npm run build && npm run start`, load `/contact`, inspect the input borders in devtools to confirm the new CSS variable is applied and the computed border color matches what you specified.
3. `git diff --stat` — confirm only the three IN-scope files changed.
4. `npm run lint`.

## CONSTRAINTS
- Follow the existing `@theme` token declaration style in `app/globals.css` exactly (see the block containing `--color-line` for the pattern to match).
- Do not introduce a Tailwind arbitrary-value one-off (e.g. `border-[#XXXXXX]` inline) — this must be a named token, consistent with how every other color in this design system is defined once in `app/globals.css` and referenced by name, per `DESIGN.md`.
- No new dependencies.

## ROLLBACK
Revert the diff across the three touched files.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.

## REWORK NOTES (cycle 1 — staff engineer review, 2026-08-08)

**Verdict: REWORK.** Everything about the implementation shape was correct — token placement, scope (exactly the 3 IN files), `fieldErrorClass` precedence preserved, gallery mirrored. The problem is the color value itself fails its own acceptance criterion.

**The chosen `#8E96A8` does not reach 3:1 contrast against white.** Independently verified with the WCAG relative-luminance formula: luminance ≈ 0.3039, contrast ≈ **2.967:1** — under the 3:1 floor. Notably, the previous implementer's own self-report *showed* essentially this same arithmetic (their intermediate luminance value matches) but then wrote the final rounded conclusion as "≈ 3.0:1," which doesn't follow from their own numbers (1.05 / 0.3536 = 2.97, not 3.0). Redo the calculation carefully and double check the final division, not just the intermediate steps.

**Use `#848CA0` instead** — independently verified at **3.365:1** against white, a comfortable margin above the 3:1 floor rather than a knife's-edge pass, and still the same subtle mid-tone gray-blue family as the existing `--color-muted` (#97A0B8) and `--color-slate` (#5A6480) tokens, consistent with `DESIGN.md`'s palette. Update `--color-border-input` in `app/globals.css` to this value. No other change should be needed — the token name, placement, and both consuming files' edits from the prior pass were correct; just fix the hex value and re-verify AC1's math with it.

Re-run AC1's contrast calculation against `#848CA0` and show the arithmetic in your report (not just the conclusion), and re-verify AC5's qualitative check (still reads as a "subtle" border, not jarring) — it does, but confirm it yourself rather than assuming the prior pass's visual judgment carries over unchanged.
