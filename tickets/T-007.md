# T-007 — Expand `Footer.tsx` with global offices rail, phone lines, and full sitemap index

**Source finding:** AUDIT_REPORT.md §PHASE 3.2 | Severity: P1 | Class: SUBOPTIMAL
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 2.0h | **Sequence:** independent

## WHAT
Redesign `components/Footer.tsx` to match the institutional caliber of a $200,000 digital consultancy:
1. **Four-Column Site Index:**
   - *Systems & Automations:* Links to `/what-we-automate`, `/what-we-automate/ai-receptionist`, `/what-we-automate/booking-and-reminders`, `/what-we-automate/intake-and-documents`.
   - *Digital Capabilities:* Links to `/digital-services`, `/digital-services/ai-agent-development`, `/digital-services/website-development`, `/digital-services/digital-marketing`.
   - *Sectors & Insights:* Links to `/industries`, `/library`, `/newsletters`, `/about`.
   - *Governance & Machine Specs:* Links to `/legal/privacy`, `/legal/terms`, `/llms.txt`, `/pricing.md`.
2. **Global Offices Locality Rail:** Render the 4 global offices from `content/site.ts:47-52` (*Toronto, ON &middot; Surrey, BC &middot; California, US &middot; Ludhiana, PB*).
3. **Direct Contact Rail:** Render both direct telephone lines (`site.phone` and `site.phoneIndia`) alongside `site.email`.
4. **Editorial Polish:** Clean typography, mono metadata labels, hairline dividers (`border-[var(--tsc-line)]`), fully responsive across mobile (390px) to desktop (1440px).

## WHY
A minimal footer with only 4 links undermines institutional trust for $50k–$200k prospective buyers. Showing global presence, technical specifications, and comprehensive service directories provides strong social proof and enhances crawl depth.

## SCOPE — FILES
IN:
- `components/Footer.tsx`

OUT:
- `components/Header.tsx` (header is working; do not touch)
- `content/site.ts` (constants are already defined; consume them)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: `components/Footer.tsx` renders all 4 global offices from `site.offices`.
- [ ] AC2: `components/Footer.tsx` renders direct telephone contact links (`site.phone` and `site.phoneIndia`).
- [ ] AC3: Includes links to machine-readable endpoints (`/llms.txt` and `/pricing.md`).
- [ ] AC4: Clean responsive grid: 2 columns on mobile, 4 columns on desktop, with zero overflow.
- [ ] AC5: `bun run test` passes with zero regressions.

## TEST PLAN
1. Inspect `http://localhost:3000` in browser. Scroll to footer.
2. Verify all 4 offices, phone numbers, and spec links render correctly.
3. Check mobile viewport (390px) to verify clean responsive stacking.

## CONSTRAINTS
- Strict compliance with `DESIGN.md` tokens.
- No new npm dependencies.

## ROLLBACK
Revert `components/Footer.tsx` via `git checkout`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
