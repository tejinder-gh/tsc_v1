# T-008 — Add `lastModified` timestamps to dynamic `sitemap.ts`

**Source finding:** AUDIT_REPORT.md §PHASE 4.2 | Severity: P1 | Class: SUBOPTIMAL
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 1.0h | **Sequence:** independent

## WHAT
Update `app/sitemap.ts` to include `lastModified` ISO timestamps on all 91 generated URLs across static routes, industries, automation services, digital services, and newsletter publications. Derive timestamps from the content files or a stable build-time date.

## WHY
Search engines (Google, Bing) and AI crawlers (Perplexity, SearchGPT) prioritize recency and crawl budget based on `lastModified` metadata. Omitting timestamps results in slower re-indexing of new services and case studies.

## SCOPE — FILES
IN:
- `app/sitemap.ts`

OUT:
- `app/robots.ts` (robots configuration is working and already tested)
- `content/*` (do not alter content arrays)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: Every entry in `app/sitemap.ts` includes `lastModified: string | Date`.
- [ ] AC2: Visiting `/sitemap.xml` returns valid XML containing `<lastmod>` tags for all 91 routes.
- [ ] AC3: `bun run build` succeeds and outputs static `/sitemap.xml`.
- [ ] AC4: `bun run test` passes with zero regressions.

## TEST PLAN
1. Run `bun run build` to verify Next.js builds sitemap successfully.
2. Run `curl -s http://localhost:3000/sitemap.xml | grep "<lastmod>"` and confirm presence of timestamps.

## CONSTRAINTS
- Strict Next.js `MetadataRoute.Sitemap` type compliance.

## ROLLBACK
Revert `app/sitemap.ts` via `git checkout`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
