# T-003 — Resolve high-severity dependency advisories

**Source finding:** AUDIT_REPORT.md Phase 0 / CTO §1B | Severity: P1 | Class: BROKEN
**Assigned tier:** SENIOR
**Effort estimate:** 2h | **Sequence:** independent

## WHAT
Run exactly one command — `npm audit fix` (no flags, no `--force`) — to resolve the 4 high-severity `npm audit` advisories (transitive through `next@16.1.0`: PostCSS XSS/path-traversal CVEs, `sharp`/libvips CVEs, plus Next.js's own SSRF-via-rewrites and internal-Server-Function-disclosure advisories), then verify the app still builds, tests, and lints clean.

**This has already been dry-run-verified by the staff engineer reviewer: `npm audit fix` alone resolves all 4 high-severity advisories, bumps `next` from `16.1.0` to `16.3.0` automatically within the existing `^16.1.0` semver range (no major bump, no `--force` needed), and `npm run build` / `npm test` both pass cleanly afterward with 0 code changes required.** Do not investigate alternate approaches, do not run `npm view`, `npm pack`, `npm diff`, `git stash`, or manually inspect candidate versions — none of that is necessary and none of those commands are available in this session anyway. If `npm audit fix` alone does not fully resolve the 4 high-severity findings in your environment, STOP immediately and report BLOCKED with the exact remaining `npm audit --omit=dev` output — do not escalate to `--force` or hand-edit version numbers.

## WHY
`npm audit` (run fresh during this audit) reports 4 high-severity vulnerabilities with fixes available. Two of the Next.js advisories specifically — unauthenticated disclosure of internal Server Function endpoints, and unbounded Server Action payload size in Edge runtime — compound directly with T-001's dashboard finding (Server Actions are exactly what's exposed there), so closing this gap raises the floor under that fix rather than being unrelated hygiene.

## SCOPE — FILES
IN:
- `package.json`
- `package-lock.json`

OUT:
- Any application code (`app/`, `components/`, `lib/`, `automations/`) — if the version bump requires code changes to keep the build green, STOP and report BLOCKED rather than adapting code to a breaking change; a breaking upgrade is a bigger, separately-scoped ticket.
- `next.config.mjs` — do not add config to work around an advisory; the fix is the version bump itself.

## ACCEPTANCE CRITERIA
- [ ] AC1: `npm audit --omit=dev` reports 0 high-severity vulnerabilities (moderate/low may remain if no fix is available — note any that can't be closed, with the specific advisory ID, in your final report).
- [ ] AC2: `npm run build` completes with no new errors or warnings that weren't present before the bump.
- [ ] AC3: `npm test` — all 85 tests still pass, no new failures.
- [ ] AC4: `npm run lint` (`biome check .`) still reports clean.
- [ ] AC5: `npm run start` after `npm run build` serves `/` and one `/what-we-automate/[slug]` page successfully (manual smoke check — App Router builds have broken on minor version bumps before; a green build is necessary but not sufficient).

## TEST PLAN
Run these commands, in this exact order, nothing else:
1. `npm audit --omit=dev` — record the baseline (4 high).
2. `npm audit fix` — the one and only remediation command for this ticket.
3. `npm audit --omit=dev` — confirm 0 high.
4. `npm run build`
5. `npm test`
6. `npm run lint`
7. `npm run start` then a manual smoke check per AC5, then stop the server.

## CONSTRAINTS
- Run only the seven commands listed in TEST PLAN, in that order. Do not run `npm view`, `npm pack`, `npm diff`, `git stash`, or any other exploratory command — they are not in this session's allowlist and are not needed; the fix has already been verified to be exactly `npm audit fix` with no follow-up required.
- Stay within the `next@16.x` line — `npm audit fix` alone stays within it (verified: resolves to `16.3.0`, still matching the existing `^16.1.0` range in `package.json`). If your run produces a different result that crosses a major version, STOP and report BLOCKED rather than proceeding.
- No new dependencies beyond what `npm audit fix` itself pulls in transitively.
- Do not use `npm audit fix --force`. If plain `npm audit fix` doesn't fully resolve the 4 high-severity findings, STOP and report BLOCKED with the exact remaining `npm audit --omit=dev` output — do not escalate to `--force` or hand-edit version numbers.

## ROLLBACK
`git checkout -- package.json package-lock.json && npm install` restores the prior dependency tree exactly.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
