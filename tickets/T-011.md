# T-011 — Restore WCAG AA 3:1 contrast on search and journey input borders

**Source finding:** AUDIT_REPORT.md DES-01 | Severity: P1 | Class: BROKEN  
**Assigned tier:** INTERMEDIATE  
**Effort estimate:** 1.5h | **Sequence:** independent

## WHAT
Restore WCAG 2.1 AA compliant non-text contrast (minimum 3:1) on the input borders of interactive user inputs across the editorial site redesign. Specifically, introduce a high-contrast input border token `--tsc-line-strong` in `app/globals.css` (measuring >= 3:1 against both `#ffffff` and `--tsc-paper` `#f4f1e9`, such as `#848CA0` or `#6D6B63`). Apply this token to the border styling in `components/journey/ProblemInput.tsx` and `features/catalog/components/SearchBar.tsx`, eliminating the low-contrast `--tsc-line` (`#d7d2c7`, 1.35:1 to 1.52:1) on interactive boundaries.

## WHY
Ticket T-007 previously corrected input border contrast on the legacy contact and booking forms to `#848CA0` (3.36:1 contrast against white). However, the recently added editorial journey components (`ProblemInput.tsx`) and catalog search (`SearchBar.tsx`) reverted to using `--tsc-line` (`#d7d2c7`), which measures only 1.35:1 against paper and 1.52:1 against white, failing WCAG 2.1 Success Criterion 1.4.11 (Non-text Contrast) and reducing usability for low-vision users.

## SCOPE — FILES
IN:
- `app/globals.css`
- `components/journey/ProblemInput.tsx`
- `features/catalog/components/SearchBar.tsx`

OUT:
- `components/forms/ContactForm.tsx`, `QuickMessageForm.tsx`, `ChecklistForm.tsx` — legacy form styles already verified under T-007; do not alter.
- `DESIGN.md` — documentation updates belong in T-014.

## ACCEPTANCE CRITERIA
- [ ] AC1: `app/globals.css` defines `--tsc-line-strong` with a verified contrast ratio >= 3.0:1 against white (`#ffffff`) and paper (`#f4f1e9`).
- [ ] AC2: `components/journey/ProblemInput.tsx` input border uses `--tsc-line-strong` and visibly passes 3:1 contrast in both default and dark states.
- [ ] AC3: `features/catalog/components/SearchBar.tsx` input border uses `--tsc-line-strong` and visibly passes 3:1 contrast against paper.
- [ ] AC4: Interactive focus rings (`focus:ring-2`, `focus:border-...`) remain distinct and high-contrast when inputs are focused.
- [ ] AC5: Non-interactive hairline dividers and card boundaries throughout the rest of the site remain on `--tsc-line` (preserving subtle editorial aesthetics).
- [ ] AC6: `npm run lint` and `npm test` pass cleanly.

## TEST PLAN
1. Compute relative luminance and contrast ratio for the selected token against `#ffffff` and `#f4f1e9` using the WCAG formula: $(L_1 + 0.05) / (L_2 + 0.05) \ge 3.0$.
2. Run `npm run lint` (`biome check .`) to verify no syntax or formatting issues.
3. Run `npm test` — all 307 tests pass.
4. Visually inspect `/` (hero problem input) and `/library` (search bar) to ensure crisp, readable borders that don't wash out.

## CONSTRAINTS
- Do not introduce ad-hoc utility colors; define the token cleanly in `app/globals.css`.
- Preserve existing font and placeholder styling.

## ROLLBACK
`git checkout -- app/globals.css components/journey/ProblemInput.tsx features/catalog/components/SearchBar.tsx`

## BLOCKED PROTOCOL
If the token change interferes with Tailwind v4 `@theme` compilation: STOP, write what failed, status → BLOCKED.
